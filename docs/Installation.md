# Installation

## Requirements

- Node.js 20+
- npm
- Postgres access for live mode

## Install

```powershell
npm install
```

## Local runtime

```powershell
npm run dev
```

If `3000` is busy:

```powershell
npm run dev -- --port 3004
```
